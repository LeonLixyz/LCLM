# Copy to .env and fill in. .env is gitignored.

# Required: HuggingFace token for downloading models / datasets.
HF_TOKEN=

# Optional: Weights & Biases logging from training workers.
WANDB_API_KEY=
